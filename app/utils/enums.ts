export enum TaskStatus {
  PENDING = "pending",
  IN_PROGRESS = "in_progress",
  COMPLETED = "completed",
  OVERDUE = "overdue",
}

export enum Priority {
  LOW = "low",
  MEDIUM = "medium",
  HIGH = "high",
}

export enum Filter {
  ALL = "all",
  PENDING = "pending",
  IN_PROGRESS = "in_progress",
  COMPLETED = "completed",
}
